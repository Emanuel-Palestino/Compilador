function a (lalo,toko){
    while ( ! b ){
        console.log ("Perras estupidas inician su nombre con D y su apellido con L")
    }
    if ( m < b || Y >= kassalask ){
        function baladas (messi,ronaldo,debruyne){
            "ATLAS ES CAMPEÓN"
        }
        console.log ("JALADAS")
    }
}
console.log ("Hola")
1+2
switch ( hols ){
    case m : l = k  
    break
    case n : l = k  
    break
    case o : l = k  
    break
    case p : l = k  
    break
    default : console.log ("opqwowkqop")
}
function a (lalo,toko){
    while ( ! b ){
        console.log ("Perras estupidas inician su nombre con D y su apellido con L")
    }
    if ( m < b || Y >= kassalask ){
        function baladas (messi,ronaldo,debruyne){
            "ATLAS ES CAMPEÓN"
        }
        console.log ("JALADAS")
    }
}
function a (lalo,toko){
    while ( ! b ){
        console.log ("Perras estupidas inician su nombre con D y su apellido con L")
    }
    if ( m < b || Y >= kassalask ){
        function baladas (messi,ronaldo,debruyne){
            "ATLAS ES CAMPEÓN"
        }
        console.log ("JALADAS")
    }
}
function a (lalo,toko){
    while ( ! b ){
        console.log ("Perras estupidas inician su nombre con D y su apellido con L")
    }
    if ( m < b || Y >= kassalask ){
        function baladas (messi,ronaldo,debruyne){
            "ATLAS ES CAMPEÓN"
        }
        console.log ("JALADAS")
    }
    function a (lalo,toko){
        while ( ! b ){
            console.log ("Perras estupidas inician su nombre con D y su apellido con L")
        }
        if ( m < b || Y >= kassalask ){
            function baladas (messi,ronaldo,debruyne){
                "ATLAS ES CAMPEÓN"
            }
            console.log ("JALADAS")
        }
    }
}
